import Foundation
/*
// For string operations (use this for Supabase)
// This is what we need
func getCurrentUserIdString() -> String? {
    return supabase.auth.currentUser.id  // Returns String
}

// Non-optional version when you're sure user is logged in
func getCurrentUserIdStringOrFail() -> UUID {
    guard let userId = supabase.auth.currentUser?.id else {
        fatalError("No authenticated user")
    }
    return userId
}
*/
